/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      "seejob.netlify.app",
      "via.placeholder.com",
      "res.cloudinary.com",
    ],
  },
};

export default nextConfig;
